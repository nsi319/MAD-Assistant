<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.appdevlab.mad">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TextComponentActivity" android:label="TextView &amp; EditText" android:parentActivityName=".MainActivity" android:theme="@style/ModuleTheme"/>
        <activity android:name=".ButtonComponentActivity" android:label="Buttons" android:parentActivityName=".MainActivity" android:theme="@style/ModuleTheme"/>
        <activity android:name=".SeekbarComponentActivity" android:label="Seekbar &amp; Progressbar" android:parentActivityName=".MainActivity" android:theme="@style/ModuleTheme"/>
        <activity android:name=".PickerActivity" android:label="Pickers" android:parentActivityName=".MainActivity" android:theme="@style/ModuleTheme"/>
        <activity android:name=".SpinnerActivity" android:label="Spinners" android:parentActivityName=".MainActivity" android:theme="@style/ModuleTheme"/>
        <activity android:name=".MenuActivity" android:label="Menu" android:parentActivityName=".MainActivity" android:theme="@style/ModuleTheme"/>
        <activity android:name=".SourceCodeActivity"  android:label="Source Code" android:theme="@style/ModuleTheme"/>
    </application>
</manifest>